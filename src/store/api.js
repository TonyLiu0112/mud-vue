const state = {
  URI: 'http://localhost:58080/api/'
}

const action = {}

const mutations = {}

const getters = {
  basicPath (state) {
    return state.URI
  }
}

export default {
  state,
  action,
  mutations,
  getters
}
